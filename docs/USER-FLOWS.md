# ClaudeOps Dashboard - User Flow Guide

> MCP_ClaudeOps v1.5 | 2026-02-09

## 목차

- [대시보드 개요](#대시보드-개요)
- [네비게이션 구조](#네비게이션-구조)
- [유저 플로우 시나리오](#유저-플로우-시나리오)
  - [시나리오 1: PRD 기반 프로젝트 시작](#시나리오-1-prd-기반-프로젝트-시작)
  - [시나리오 2: 팀 구성 및 작업 할당](#시나리오-2-팀-구성-및-작업-할당)
  - [시나리오 3: 칸반 보드에서 작업 관리](#시나리오-3-칸반-보드에서-작업-관리)
  - [시나리오 4: Git Worktree 기반 개발](#시나리오-4-git-worktree-기반-개발)
  - [시나리오 5: 비용 모니터링 및 예산 관리](#시나리오-5-비용-모니터링-및-예산-관리)
  - [시나리오 6: 일일 운영 리포트 생성](#시나리오-6-일일-운영-리포트-생성)
- [페이지별 상세 플로우](#페이지별-상세-플로우)
- [API 엔드포인트 매핑](#api-엔드포인트-매핑)

---

## 대시보드 개요

ClaudeOps Dashboard는 AI 에이전트 운영을 통합 관리하는 웹 대시보드입니다.

```
http://localhost:48391  (Dashboard)
http://localhost:48390  (Backend API)
```

**주요 기능 영역:**
- **프로젝트 관리**: PRD → Epic → Task → Worktree 계층 구조로 프로젝트 라이프사이클 관리
- **팀 관리**: 팀 생성, 멤버 관리, 작업 할당, 워크로드 시각화
- **운영 모니터링**: 세션, 에이전트, 도구, 이벤트 실시간 추적
- **비용 관리**: 토큰 사용량, 비용 추적, 예산 알림

---

## 네비게이션 구조

사이드바는 5개 그룹으로 구성되며, 각 그룹은 접기/펼치기가 가능합니다.

```
📊 커맨드 센터          /
─── 프로젝트 관리 ───
📋 제품 요구사항 (PRD)  /prds
🎯 실행 단위 (에픽)     /epics
📌 작업 보드            /tasks
🌳 개발 브랜치          /worktrees
─── 팀 ───
👥 팀 & 멤버           /teams
─── 운영 모니터링 ───
💻 세션 모니터          /sessions
🤖 에이전트 활동        /agents
🔧 도구 분석           /tools
📡 이벤트 로그          /events
─── 비용 & 리포트 ───
💰 비용 현황            /tokens
📊 운영 리포트          /reports
⚙️ 설정                /settings
```

---

## 유저 플로우 시나리오

### 시나리오 1: PRD 기반 프로젝트 시작

> **역할**: 프로덕트 매니저 / 테크 리드
> **목표**: 새 기능을 PRD로 정의하고 Epic/Task로 분해하여 실행 계획 수립

```
┌─────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  1. PRD  │────▶│  2. Epic  │────▶│  3. Task  │────▶│ 4. Board │
│  작성    │     │  분해     │     │  생성     │     │  관리    │
└─────────┘     └──────────┘     └──────────┘     └──────────┘
```

**Step 1: PRD 작성** (`/prds`)
1. 사이드바에서 "제품 요구사항 (PRD)" 클릭
2. 우측 상단 "New PRD" 버튼 클릭
3. PRD 생성 다이얼로그에 입력:
   - **제목**: "Team 관리 시스템"
   - **설명**: "팀 구성, 멤버 관리, 작업 할당 기능"
   - **비전**: "효율적인 AI 에이전트 팀 운영"
   - **사용자 스토리**: "팀 리드로서 멤버에게 작업을 할당하고 싶다"
   - **성공 기준**: "팀 CRUD, 멤버 관리, 워크로드 시각화"
4. "Create" 클릭 → PRD 카드가 목록에 추가됨

**Step 2: Epic 분해** (`/epics`)
1. "실행 단위 (에픽)" 페이지로 이동
2. "New Epic" 클릭
3. Epic 생성 다이얼로그:
   - **PRD 선택**: 방금 만든 "Team 관리 시스템"
   - **제목**: "백엔드 API 구현"
   - **설명**: "Team CRUD, 멤버 관리 REST API"
   - **기술 접근**: "Fastify + SQLite"
4. 같은 방법으로 "프론트엔드 UI", "MCP 도구 통합" Epic 추가 생성
5. PRD 필터 드롭다운에서 해당 PRD 선택 → 관련 Epic만 표시

**Step 3: Task 생성** (`/tasks`)
1. "작업 보드" 페이지로 이동
2. "New Task" 클릭
3. Task 생성 다이얼로그:
   - **제목**: "teams 테이블 스키마 설계"
   - **설명**: "teams, team_members, task_assignees 테이블 DDL 작성"
   - **우선순위**: P1
   - **Epic**: "백엔드 API 구현"
   - **예상 공수**: "2h"
4. 필요한 만큼 Task 반복 생성
5. Epic 필터로 특정 Epic의 Task만 확인 가능

**Step 4: 대시보드에서 진행 상황 확인** (`/`)
1. 커맨드 센터로 이동
2. PRD/Epic 위젯에서 전체 진행률 확인
3. Task Stats 위젯에서 완료율 확인

---

### 시나리오 2: 팀 구성 및 작업 할당

> **역할**: 테크 리드 / 팀 매니저
> **목표**: 팀을 만들고 멤버를 추가한 뒤, 작업을 적절히 할당

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  1. 팀   │────▶│ 2. 멤버  │────▶│ 3. 작업  │────▶│ 4. 워크  │
│  생성    │     │  추가    │     │  할당    │     │  로드확인│
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

**Step 1: 팀 생성** (`/teams`)
1. 사이드바에서 "팀 & 멤버" 클릭
2. "Create Team" 버튼 클릭
3. 팀 생성 다이얼로그:
   - **이름**: "Backend Squad"
   - **설명**: "백엔드 개발팀"
   - **색상**: 파란색 선택
4. "Create" → 팀 카드가 상단에 추가됨

**Step 2: 멤버 추가** (`/teams` → 팀 선택)
1. 방금 만든 "Backend Squad" 카드 클릭 (선택 상태)
2. 왼쪽 멤버 리스트 영역에서 "+ Add" 클릭
3. 멤버 추가 다이얼로그:
   - **이름**: "김개발"
   - **역할**: Lead
   - **이메일**: dev@company.com
   - **전문 분야**: "backend, typescript, database"
4. "Add" 클릭 → 멤버가 리스트에 추가됨
5. 같은 방법으로 "박프론트", "이데이터" 추가

**Step 3: 작업 할당** (`/tasks/[id]`)
1. "작업 보드" → 할당할 Task 카드 클릭
2. Task 상세 페이지에서 "Assignees" 섹션 확인
3. "Add Member" 버튼 클릭
4. 드롭다운에서 "김개발" 선택 → 즉시 할당됨
5. 필요시 "박프론트"도 추가 할당 (다중 할당 가능)
6. 할당 해제: 멤버 아바타에 마우스 호버 → X 클릭

**Step 4: 워크로드 확인** (`/teams`)
1. "팀 & 멤버" 페이지로 돌아가기
2. 팀 선택 → 워크로드 히트맵에서 팀 전체 부하 확인
3. 개별 멤버 클릭 → 해당 멤버의 할당된 작업 목록 확인
4. 상태별/우선순위별 작업 분포 확인
5. 커맨드 센터의 "팀 워크로드" 위젯에서도 요약 확인 가능

---

### 시나리오 3: 칸반 보드에서 작업 관리

> **역할**: 개발자
> **목표**: 할당된 작업을 칸반 보드에서 진행 상태 관리

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ Backlog  │────▶│  To Do   │────▶│   진행   │────▶│   완료   │
│          │     │          │     │   중     │     │          │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

**작업 보드 사용** (`/tasks`)
1. 칸반 보드에 5개 컬럼 표시:
   - **Backlog**: 아직 시작 안 한 작업
   - **To Do**: 다음에 할 작업
   - **In Progress**: 진행 중인 작업
   - **Review**: 리뷰 대기
   - **Done**: 완료
2. Epic 필터로 특정 Epic의 작업만 표시 가능
3. 각 카드에 우선순위 배지, 할당자 아바타 표시
4. 보드 데이터는 **10초마다** 자동 새로고침

**작업 상태 변경**
1. Task 카드를 드래그하여 다른 컬럼으로 이동
2. 또는 Task 상세에서 상태 직접 변경

**Task 상세 확인** (`/tasks/[id]`)
1. Task 카드 클릭 → 상세 페이지
2. 확인 가능 항목:
   - 제목, 설명, 우선순위, 상태
   - 할당된 멤버 (추가/제거 가능)
   - 마감일
   - 라벨
   - 연결된 세션
   - 변경 이력 (필드명, 이전값, 변경값, 시간)
3. 삭제: 우측 상단 휴지통 아이콘 → 확인 다이얼로그

---

### 시나리오 4: Git Worktree 기반 개발

> **역할**: 개발자
> **목표**: Epic 단위로 Git worktree를 생성하여 격리된 환경에서 개발

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ Worktree │────▶│  개발    │────▶│  Merge   │────▶│  정리    │
│  생성    │     │  작업    │     │          │     │          │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

**Step 1: Worktree 생성** (`/worktrees`)
1. "개발 브랜치" 페이지로 이동
2. "New Worktree" 클릭
3. Worktree 생성 다이얼로그:
   - **이름**: "feature/team-management"
   - **프로젝트 경로**: "/Users/dev/project"
   - **Epic**: "백엔드 API 구현" (선택)
4. "Create" → Active 상태의 worktree 카드 생성

**Step 2: 개발 작업**
1. 생성된 worktree 경로에서 실제 개발 수행
2. 대시보드에서 worktree 상태 확인 (Active)
3. 관련 Task를 "In Progress"로 이동

**Step 3: Merge** (`/worktrees`)
1. 개발 완료 후 worktree 카드의 "Merge" 버튼 클릭
2. 상태가 "Active" → "Merged"로 변경 (파란색 배지)

**Step 4: 정리** (`/worktrees`)
1. 더 이상 필요 없는 worktree의 "Remove" 클릭
2. 상태가 "Removed"로 변경 (회색 배지)
3. 상태 필터 탭으로 Active/Merged/Removed 별도 확인 가능

---

### 시나리오 5: 비용 모니터링 및 예산 관리

> **역할**: 운영 관리자
> **목표**: AI 에이전트 사용 비용을 추적하고 예산 한도 설정

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  비용    │────▶│  예산    │────▶│  세션별  │────▶│ 에이전트│
│  현황    │     │  설정    │     │  분석    │     │  최적화 │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
```

**Step 1: 전체 비용 확인** (`/tokens`)
1. "비용 현황" 페이지로 이동
2. 3개 요약 카드 확인:
   - Total Input Tokens (최근 30일)
   - Total Output Tokens (최근 30일)
   - Total Cost (USD)
3. 모델별 분석: Haiku / Sonnet / Opus 각각의 토큰 사용량

**Step 2: 예산 알림 설정** (`/settings`)
1. "설정" 페이지로 이동
2. "Budget Alerts" 섹션:
   - **일일 한도**: $50.00
   - **월간 한도**: $500.00
3. "Save Budget Alerts" 클릭
4. `/tokens` 페이지에서 예산 진행 바 확인 가능

**Step 3: 세션별 비용 분석** (`/sessions`)
1. "세션 모니터" 페이지에서 세션 목록 확인
2. 각 세션의 토큰 수, 비용 확인
3. 세션 ID 클릭 → 상세 정보 확인

**Step 4: 에이전트 효율 분석** (`/agents`)
1. "에이전트 활동" 페이지에서 에이전트 성능 테이블 확인
2. 컬럼: Agent Type, Model, Calls, Avg Duration, Success Rate, Cost
3. 높은 비용의 에이전트 식별 → 모델 다운그레이드 검토

---

### 시나리오 6: 일일 운영 리포트 생성

> **역할**: 팀 리드 / 운영 관리자
> **목표**: 하루의 작업 요약 리포트를 자동 생성

```
┌──────────┐     ┌──────────┐     ┌──────────┐
│ 리포트   │────▶│ 스탠드업 │────▶│  확인    │
│ 페이지   │     │  생성    │     │  & 공유  │
└──────────┘     └──────────┘     └──────────┘
```

**Step 1: 리포트 페이지** (`/reports`)
1. "운영 리포트" 페이지로 이동
2. 좌측: 기존 리포트 목록 (리포트 타입, 세션 ID, 생성일)
3. 우측: 선택된 리포트 뷰어

**Step 2: 스탠드업 생성**
1. "Generate Standup" 버튼 클릭
2. 버튼이 "Generating..." 로딩 상태로 변경
3. AI가 최근 세션/이벤트를 분석하여 리포트 자동 생성
4. 완료 시 리포트 목록에 새 항목 추가

**Step 3: 리포트 확인**
1. 새로 생성된 리포트 카드 클릭
2. 우측 뷰어에 리포트 내용 표시
3. 리포트 데이터는 60초마다 자동 새로고침

---

## 페이지별 상세 플로우

### `/` 커맨드 센터 (홈)

| 위젯 | 표시 내용 | 새로고침 |
|------|-----------|----------|
| Live Indicator | 실시간 상태 배지 | 실시간 |
| Overview Cards | 4개 핵심 지표 | 자동 |
| Cost Summary | 비용 추적 | 자동 |
| Task Stats | 작업 완료율 | 30초 |
| PRD/Epic Widget | 프로젝트 진행률 | 자동 |
| Team Workload | 팀 부하 현황 | 자동 |

### `/prds` 제품 요구사항

| 액션 | 방법 | API |
|------|------|-----|
| PRD 목록 조회 | 페이지 진입 시 자동 | `GET /api/prds` |
| 상태별 필터 | 탭 클릭 (All/Backlog/Active/Completed/Archived) | Query param |
| PRD 생성 | "New PRD" → 다이얼로그 → Create | `POST /api/prds` |
| PRD 상세 | 카드 클릭 → `/prds/[id]` | `GET /api/prds/:id` |

### `/epics` 실행 단위

| 액션 | 방법 | API |
|------|------|-----|
| Epic 목록 조회 | 페이지 진입 시 자동 | `GET /api/epics` |
| PRD별 필터 | PRD 드롭다운 선택 | Query param |
| 상태별 필터 | 탭 클릭 | Query param |
| Epic 생성 | "New Epic" → 다이얼로그 | `POST /api/epics` |
| Epic 상세 | 카드 클릭 → `/epics/[id]` | `GET /api/epics/:id` |

### `/tasks` 작업 보드

| 액션 | 방법 | API |
|------|------|-----|
| 칸반 보드 조회 | 페이지 진입 (10초 새로고침) | `GET /api/tasks/board` |
| Epic 필터 | 드롭다운 선택 | Query param |
| Task 생성 | "New Task" → 다이얼로그 | `POST /api/tasks` |
| Task 이동 | 카드 드래그 또는 상태 변경 | `PATCH /api/tasks/:id/move` |
| Task 상세 | 카드 클릭 → `/tasks/[id]` | `GET /api/tasks/:id` |

### `/tasks/[id]` 작업 상세

| 액션 | 방법 | API |
|------|------|-----|
| 멤버 할당 | "Add Member" → 드롭다운 선택 | `POST /api/tasks/:id/assign` |
| 멤버 해제 | 아바타 호버 → X 클릭 | `DELETE /api/tasks/:id/assign` |
| Task 삭제 | 휴지통 아이콘 → 확인 | `DELETE /api/tasks/:id` |
| 변경 이력 | 자동 표시 | `GET /api/tasks/:id/history` |

### `/worktrees` 개발 브랜치

| 액션 | 방법 | API |
|------|------|-----|
| Worktree 목록 | 페이지 진입 시 자동 | `GET /api/worktrees` |
| 상태 필터 | 탭 클릭 (All/Active/Merged/Removed) | Query param |
| Worktree 생성 | "New Worktree" → 다이얼로그 | `POST /api/worktrees` |
| Merge | "Merge" 버튼 클릭 | `POST /api/worktrees/:id/merge` |
| Remove | "Remove" 버튼 클릭 | `POST /api/worktrees/:id/remove` |

### `/teams` 팀 & 멤버

| 액션 | 방법 | API |
|------|------|-----|
| 팀 목록 조회 | 페이지 진입 (30초 새로고침) | `GET /api/teams` |
| 팀 생성 | "Create Team" → 다이얼로그 | `POST /api/teams` |
| 팀 삭제 | 카드의 삭제 버튼 | `DELETE /api/teams/:id` |
| 팀 선택 | 카드 클릭 → 멤버 패널 표시 | - |
| 멤버 추가 | "+ Add" → 다이얼로그 | `POST /api/teams/:id/members` |
| 멤버 삭제 | 멤버 항목의 제거 버튼 | `DELETE /api/members/:id` |
| 멤버 워크로드 | 멤버 클릭 → 상세 패널 | `GET /api/members/:id/workload` |
| 팀 워크로드 | 팀 선택 시 하단 히트맵 | `GET /api/teams/:id/workload` |

### `/sessions` 세션 모니터

| 액션 | 방법 | API |
|------|------|-----|
| 세션 목록 | 테이블 자동 로드 | `GET /api/sessions` |
| 세션 상세 | 세션 ID 클릭 → `/sessions/[id]` | `GET /api/sessions/:id` |

### `/agents` 에이전트 활동

| 액션 | 방법 | API |
|------|------|-----|
| 성능 테이블 | 자동 로드 (읽기 전용) | `GET /api/agents/stats` |

### `/events` 이벤트 로그

| 액션 | 방법 | API |
|------|------|-----|
| 이벤트 스트림 | 자동 (5초 새로고침) | `GET /api/events` |

### `/tokens` 비용 현황

| 액션 | 방법 | API |
|------|------|-----|
| 토큰 사용량 | 자동 로드 (30일) | `GET /api/tokens/usage?days=30` |
| 예산 진행률 | 자동 표시 | `GET /api/tokens/budget` |

### `/reports` 운영 리포트

| 액션 | 방법 | API |
|------|------|-----|
| 리포트 목록 | 자동 (60초 새로고침) | `GET /api/reports` |
| 리포트 보기 | 카드 클릭 → 뷰어 | - |
| 스탠드업 생성 | "Generate Standup" 클릭 | `POST /api/reports/standup` |

### `/settings` 설정

| 섹션 | 설정 항목 | API |
|------|-----------|-----|
| 가격 정책 | Haiku/Sonnet/Opus 단가 (읽기 전용) | `GET /api/config` |
| 예산 알림 | 일일/월간 한도 | `PUT /api/tokens/budget-alerts` |
| GitHub 연동 | owner/repo, sync 옵션 | `PUT /api/github/config` |
| 프로젝트 컨텍스트 | brief/tech/architecture/rules | `POST/DELETE /api/contexts` |

---

## API 엔드포인트 매핑

### Project Management
```
GET    /api/prds                  # PRD 목록
POST   /api/prds                  # PRD 생성
GET    /api/prds/:id              # PRD 상세
PATCH  /api/prds/:id              # PRD 수정

GET    /api/epics                 # Epic 목록
POST   /api/epics                 # Epic 생성
GET    /api/epics/:id             # Epic 상세
PATCH  /api/epics/:id             # Epic 수정

GET    /api/tasks                 # Task 목록
GET    /api/tasks/board           # 칸반 보드 데이터
POST   /api/tasks                 # Task 생성
GET    /api/tasks/:id             # Task 상세
PATCH  /api/tasks/:id             # Task 수정
DELETE /api/tasks/:id             # Task 삭제
PATCH  /api/tasks/:id/move        # Task 이동 (칸반)
GET    /api/tasks/:id/history     # Task 변경 이력
POST   /api/tasks/:id/assign      # 멤버 할당
DELETE /api/tasks/:id/assign      # 멤버 해제
```

### Team Management
```
GET    /api/teams                 # 팀 목록
POST   /api/teams                 # 팀 생성
GET    /api/teams/:id             # 팀 상세 (멤버 포함)
DELETE /api/teams/:id             # 팀 삭제
POST   /api/teams/:id/members     # 멤버 추가
GET    /api/teams/:id/workload    # 팀 워크로드

GET    /api/members               # 멤버 목록
PATCH  /api/members/:id           # 멤버 수정
DELETE /api/members/:id           # 멤버 삭제
GET    /api/members/:id/workload  # 멤버 워크로드
```

### Worktrees
```
GET    /api/worktrees             # Worktree 목록
POST   /api/worktrees             # Worktree 생성
POST   /api/worktrees/:id/merge   # Worktree 병합
POST   /api/worktrees/:id/remove  # Worktree 제거
```

### Operations
```
GET    /api/sessions              # 세션 목록
GET    /api/sessions/:id          # 세션 상세
GET    /api/agents/stats          # 에이전트 성능
GET    /api/events                # 이벤트 로그
GET    /api/tokens/usage          # 토큰 사용량
GET    /api/tokens/budget         # 예산 정보
PUT    /api/tokens/budget-alerts  # 예산 알림 설정
GET    /api/reports               # 리포트 목록
POST   /api/reports/standup       # 스탠드업 생성
GET    /api/config                # 설정 조회
GET    /api/github/config         # GitHub 설정
PUT    /api/github/config         # GitHub 설정 수정
```

### Health
```
GET    /health                    # 서버 상태 확인
```

---

## 데이터 새로고침 주기

| 데이터 | 주기 | 페이지 |
|--------|------|--------|
| 칸반 보드 | 10초 | `/tasks` |
| 이벤트 로그 | 5초 | `/events` |
| Task 통계 | 30초 | `/`, `/tasks` |
| 팀 목록 | 30초 | `/teams` |
| 리포트 목록 | 60초 | `/reports` |
| 기타 목록 | 30초 | 각 목록 페이지 |

---

## 컴포넌트 구조

```
src/
├── app/                          # 페이지 라우트
│   ├── page.tsx                  # 커맨드 센터
│   ├── tasks/
│   │   ├── page.tsx              # 칸반 보드
│   │   └── [id]/page.tsx         # Task 상세
│   ├── teams/page.tsx            # 팀 관리
│   ├── prds/
│   │   ├── page.tsx              # PRD 목록
│   │   └── [id]/page.tsx         # PRD 상세
│   ├── epics/
│   │   ├── page.tsx              # Epic 목록
│   │   └── [id]/page.tsx         # Epic 상세
│   ├── worktrees/page.tsx        # Worktree 관리
│   ├── sessions/
│   │   ├── page.tsx              # 세션 목록
│   │   └── [id]/page.tsx         # 세션 상세
│   ├── agents/page.tsx           # 에이전트 성능
│   ├── events/page.tsx           # 이벤트 로그
│   ├── tokens/page.tsx           # 비용 현황
│   ├── reports/page.tsx          # 운영 리포트
│   ├── tools/page.tsx            # 도구 분석
│   └── settings/page.tsx         # 설정
├── components/
│   ├── layout/
│   │   ├── sidebar.tsx           # 사이드바 (접기/그룹 토글)
│   │   ├── header.tsx            # 상단 바
│   │   └── nav-items.ts          # 네비게이션 정의
│   ├── tasks/
│   │   ├── kanban-board.tsx      # 칸반 보드
│   │   ├── kanban-column.tsx     # 칸반 컬럼
│   │   ├── task-card.tsx         # Task 카드
│   │   ├── task-create-dialog.tsx # Task 생성 다이얼로그
│   │   └── priority-badge.tsx    # 우선순위 배지
│   ├── teams/
│   │   ├── team-card.tsx         # 팀 카드
│   │   ├── team-create-dialog.tsx # 팀 생성
│   │   ├── member-list.tsx       # 멤버 리스트
│   │   ├── member-avatar.tsx     # 멤버 아바타
│   │   ├── member-create-dialog.tsx # 멤버 추가
│   │   ├── member-task-list.tsx  # 멤버 작업 목록
│   │   ├── workload-stats.tsx    # 워크로드 통계
│   │   ├── workload-heatmap.tsx  # 워크로드 히트맵
│   │   └── team-workload-widget.tsx # 대시보드 위젯
│   └── ...
├── lib/hooks/                    # React Query hooks
│   ├── use-tasks.ts              # Task CRUD + 보드 + 이력
│   ├── use-teams.ts              # Team/Member CRUD + 워크로드
│   ├── use-prds.ts               # PRD CRUD
│   ├── use-epics.ts              # Epic CRUD
│   ├── use-worktrees.ts          # Worktree CRUD + merge/remove
│   └── use-reports.ts            # 리포트 + 스탠드업 생성
└── stores/
    └── ui-store.ts               # Zustand (사이드바, 그룹 토글)
```
